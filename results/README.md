# Results

`results/generated/` is ignored and contains local reruns.

Small, final, fully reproducible summary tables may be committed outside `generated/` once an experiment is frozen. Every committed result should name the script/configuration and input dataset version that produced it.
