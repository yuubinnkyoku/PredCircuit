name: FlyVis strict residual controls

on:
  push:
    paths:
      - .github/workflows/flyvis-strict-residual-controls.yml
      - scripts/run_flyvis_strict_residual_controls.py
      - scripts/run_flyvis_branched_residual_structure.py
      - scripts/diagnose_flyvis_branch_nudge_alignment.py
      - scripts/diagnose_flyvis_temporal_ce_credit_alignment.py
      - scripts/run_flyvis_retinotopic_temporal_adam.py
      - scripts/run_flyvis_credit_residual_scaling.py
      - src/predcircuit/**
  workflow_dispatch:

jobs:
  train:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        rule:
          - local
          - edge_permuted
          - type_pair_edge_permuted
          - offset_edge_permuted
          - source_position_edge_permuted
          - bias_permuted
          - node_type_bias_permuted
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
      - run: uv python install 3.11
      - run: uv sync
      - name: Train strict residual control ${{ matrix.rule }}
        run: >-
          uv run python scripts/run_flyvis_strict_residual_controls.py
          --rule ${{ matrix.rule }}
          --seeds 5
          --epochs 100
          --nudge-steps 16
          --learning-rate 0.01
          --test-repeats 8
          --out results/generated/flyvis_strict_residual_control_${{ matrix.rule }}.csv
      - uses: actions/upload-artifact@v4
        with:
          name: flyvis-strict-residual-control-${{ matrix.rule }}
          path: results/generated/flyvis_strict_residual_control_${{ matrix.rule }}.csv
