name: sPC width-64 relaxation budget extension

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/spc-width64-budget-extension.yml
      - scripts/diagnose_spc_width64_budget_extension.py
      - scripts/diagnose_pcalm_width64_budget.py
      - scripts/diagnose_pcalm_matched_credit_budget.py
      - src/predcircuit/pcalm.py

jobs:
  seed:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        seed: [840, 841, 842, 843, 844]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: >-
          uv run python scripts/diagnose_spc_width64_budget_extension.py
          --seed ${{ matrix.seed }}
          --out results/generated/spc_width64_budget_extension_seed${{ matrix.seed }}.csv
      - uses: actions/upload-artifact@v4
        with:
          name: spc-width64-budget-extension-seed-${{ matrix.seed }}
          path: results/generated/spc_width64_budget_extension_seed${{ matrix.seed }}.csv
          if-no-files-found: error
